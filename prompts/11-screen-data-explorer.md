# Prompt 11 — Screen: Data Explorer

---

Read `.agent.md` first. Build the `/data` screen:

- Render `public/data/forecast.json` as a sortable, scrollable table (month, actual
  orders, predicted orders, actual revenue, predicted revenue).
- Add a simple CSV download button (client-side, no backend needed) so the data can
  be attached as evidence in the submission.
- Reuse the shared disclaimer banner.

Commit as `feat(dashboard): build data explorer screen` and push.
Update `.agent.md`, set next prompt to `prompts/12-screen-recommendations.md`, commit, push.
