# Prompt 15 — Responsive & Styling Pass

---

Read `.agent.md` first. Do a full pass over every screen built in Prompts 9–14:

- Confirm mobile (375px), tablet (768px), and desktop (1280px+) all look intentional,
  not just "doesn't overflow." Check nav collapses sensibly on mobile.
- Establish one consistent type scale, spacing scale, and a small color palette
  (pick something that doesn't look like default Tailwind gray-500-on-white —
  give it a point of view, e.g. a warm accent color fitting a restaurant/food theme).
- Confirm the disclaimer banner is visually consistent across all screens.
- Run `npm run build` again to confirm nothing broke.

Commit as `style(dashboard): responsive and visual polish pass` and push.
Update `.agent.md`, set next prompt to `prompts/16-business-recommendations-memo.md`, commit, push.
