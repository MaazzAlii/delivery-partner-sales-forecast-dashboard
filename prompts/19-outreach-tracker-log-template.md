# Prompt 19 — Outreach Tracker Log

---

Read `.agent.md` first. Create `docs/outreach_tracker.csv` with columns:
`organization, platform, date_contacted, message_sent, response, response_date,
follow_up_needed, notes`. Pre-fill the `organization` column from
`docs/target_organizations.md`, leave the rest blank for me to fill in by hand
as I actually make contact (this must reflect real activity, not simulated
responses). If the shared Group 56 tracker is a separate spreadsheet outside this
repo, note that explicitly at the top of the file so I remember to log there too.

Commit as `docs(outreach): create outreach tracker log template` and push.
Update `.agent.md`, set next prompt to `prompts/20-weekly-progress-report-template.md`, commit, push.
